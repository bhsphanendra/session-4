#include <stdio.h>
#include <math.h>
int power(int num, int p){
  // Write your code here
  int v=1,i;
  if(p<0)
  {
    return -1;
  }
  else
  {
  for(i=1;i<=p;i++)
  v=v*num;
    return v;
  }
}
int main(){
  int a,b;
  scanf("%d%d",&a,&b);
  printf("%d",power(a,b));
  return 0;
}
